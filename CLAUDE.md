# CLAUDE.md

Read `AGENT.md` first — it is the single source of truth for developing this repo
(commands, conventions, verification gate, environment quirks, secrets policy).
Then read `ROADMAP.md` for current milestone status and next steps.
Full product spec: `docs/MASTER_PROMPT.md`.
