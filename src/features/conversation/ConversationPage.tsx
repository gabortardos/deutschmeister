import { MilestoneStub } from '../../app/ui'

export default function ConversationPage() {
  return (
    <MilestoneStub
      milestone="Milestone 3"
      title="Conversation"
      points={[
        '11 role-play scenarios — everyday life, work, and the Fitnessstudio (machines, duration, weights/reps/sets)',
        'Speak or type your answers; the AI tutor replies in level-appropriate German',
        'Structured mistake report after every session with corrections',
        'Requires an AI key configured in Settings → AI Model',
      ]}
    />
  )
}
