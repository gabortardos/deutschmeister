# Supabase client credentials — PUBLIC BY DESIGN (safety = Row-Level Security, not secrecy).
# Never put the service_role key or any API key in this file.
# Copy to .env for local dev; CI/deploy read GitHub repo VARIABLES of the same names.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-key>
